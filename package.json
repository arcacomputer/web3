{
  "name": "web3-arca-computer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "check": "astro check && node scripts/validate-site.mjs",
    "check:links": "node scripts/check-links.mjs",
    "build": "astro build",
    "test:e2e": "playwright test",
    "preview": "npm run build && wrangler pages dev dist",
    "deploy": "npm run build && wrangler pages deploy dist --project-name arca-web3 --branch main"
  },
  "dependencies": {
    "astro": "7.2.7"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@astrojs/check": "0.9.10",
    "@playwright/test": "1.62.1",
    "sharp": "0.35.4",
    "typescript": "6.0.3",
    "wrangler": "4.126.0"
  }
}
